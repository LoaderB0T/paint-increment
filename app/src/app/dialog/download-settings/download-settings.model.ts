export type DownloadSettings = {
  readonly accentColor: string;
  readonly columnCount: number;
  readonly transparentBackground: boolean;
  readonly renderYear: boolean;
  readonly renderEdges: boolean;
};
