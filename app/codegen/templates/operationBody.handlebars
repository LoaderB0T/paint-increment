{{{bodyMethodTsComments}}}{{methodName}}({{>operationParameters}}): Promise<{{{resultType}}}> {

    return this.{{responseMethodName}}(params).then((r: {{@root.responseClass}}<{{{resultType}}}>) => r.body as {{{resultType}}}
    );
  }
